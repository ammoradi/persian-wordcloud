/*** * this util finds the number of duplication of an item in list * will return { arrayItem : count } */export default function wordCounter(array) {    let array_copy = array;    let out_obj = {};    let out_array = [];    array_copy.map(item => {        let count = 0;        if (!out_obj[item]){            for ( let i=0; i < array_copy.length; i++) {                if( item === array_copy[i] ) count++;            }            out_obj[item] = count;            out_array.push({'text':item, 'size':count})        }    });    return out_array;}